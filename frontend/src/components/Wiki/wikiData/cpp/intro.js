// 简介章：6 个分支小节
export const introChapter = {
  id: 'intro',
  title: '简介',
  sections: [
    {
      id: 'intro-1',
      title: 'C++ 是什么',
      content: `## 一个 40 多岁的"老将"

C++ 由丹麦人 **Bjarne Stroustrup（本贾尼·斯特劳斯特卢普）** 于 1979 年在贝尔实验室发明，
最初叫 "C with Classes"（带类的 C），1983 年改名 C++。

你可以这样理解：**C++ 是 C 语言的"增强版"**——既保留了 C 的高性能和对硬件的精确控制，
又加入了面向对象、泛型编程等现代思想。

### 它厉害在哪里？

- ⚡ **快**：编译成机器码直接运行，性能接近极限，是游戏引擎、操作系统、高频交易的首选；
- 🎛 **可控**：能精确管理内存，不想要运行时开销的地方可以完全不要；
- 🌍 **无处不在**：Chrome、微信、QQ、游戏（王者荣耀/原神）、无人机飞控、自动驾驶……
  你身边的软件几乎都有 C++ 的身影；
- 🏆 **长期霸榜**：几十年稳居 TIOBE 编程语言排行榜前五。

### C 与 C++ 的关系

\`\`\`cpp
// 这段代码既是合法的 C，也是合法的 C++（C++ 兼容大部分 C）
#include <stdio.h>
int main() {
    printf("Hello\\n");
    return 0;
}
\`\`\`

> 💡 本站同时支持 C（gcc）与 C++（g++）两种语言的编译运行，.c 文件用 gcc、.cpp 用 g++。
> 学 C++ 之前不需要先学 C，本教程从零开始。`,
    },
    {
      id: 'intro-2',
      title: '程序是怎么运行的（编译 vs 解释）',
      content: `## 计算机只认识 0 和 1

你写的代码人类看得懂，但 CPU 只认识机器指令（一串 0/1）。
把"人类语言"变成"机器语言"有两种完全不同的路线：

### 路线一：编译（C++ 走这条路）

\`\`\`mermaid
源码 main.cpp → [编译器 g++] → 目标文件 → [链接器] → 可执行程序 → 运行
\`\`\`

1. **编译**：编译器检查语法、翻译成机器指令（.o 目标文件）；
2. **链接**：链接器把目标文件和库文件（如标准库）合并成最终的可执行文件；
3. **运行**：执行时不再需要编译器，程序独立运行，速度飞快。

\`\`\`bash
g++ main.cpp -o main   # 编译 + 链接，生成可执行文件 main
./main                 # 直接运行
\`\`\`

### 路线二：解释（Python 走这条路）

\`\`\`mermaid
源码 main.py → [解释器逐行翻译执行] → 输出
\`\`\`

Python 每次运行都要"边翻译边执行"，所以通常比编译型语言慢。

### 关键区别

| 对比项 | C++（编译型） | Python（解释型） |
|--------|--------------|-----------------|
| 翻译时机 | 运行前一次完成 | 运行时逐行进行 |
| 运行速度 | 极快 | 相对慢 |
| 错误发现 | 编译时就报错 | 运行到那一行才报错 |
| 发布方式 | 分发可执行文件 | 需要解释器环境 |

> 💡 **编译错误 vs 运行错误**：语法写错（少个分号、拼错关键字）会在编译阶段被拦下；
> 逻辑写错（算错了结果）编译能通过，运行才暴露。本站的"运行"按钮会同时帮你完成编译+运行两步。`,
    },
    {
      id: 'intro-3',
      title: '环境安装',
      content: `## 方式一：用本站（零安装，推荐新手）

新建一个 \`.cpp\` 文件就能直接在线编译运行，无需安装任何软件。
点击右下角 ▶ 运行，输入数据在「输入」标签页填写。

## 方式二：本地安装（想离线学习再装）

### Windows

- 方案 A（简单）：安装 **MinGW-w64**（gcc/g++ 的 Windows 版）；
  - 下载后解压，把 \`bin\` 目录加入系统环境变量 PATH；
  - 验证：终端输入 \`g++ --version\` 能显示版本号即成功。
- 方案 B（更省事）：直接安装 **Visual Studio Community**，勾选"使用 C++ 的桌面开发"。

### macOS

打开"终端"，输入以下命令安装命令行工具（含 clang++/g++）：

\`\`\`bash
xcode-select --install
\`\`\`

### Linux

\`\`\`bash
sudo apt install g++      # Ubuntu / Debian
sudo dnf install gcc-c++  # Fedora
\`\`\`

## 方式三：本地编辑器推荐

- **VS Code**（免费，装 "C/C++" 扩展，配合上面的编译器使用）；
- **CLion**（JetBrains 出品，学生可免费申请，开箱即用）；
- 新手初期用本站就够了，先学语言本身，工具以后再折腾。

> ⚠️ 如果本地运行报"找不到 g++"，99% 是 PATH 没配好或没安装编译器，编辑器不是编译器！`,
    },
    {
      id: 'intro-4',
      title: '第一个程序逐行解读',
      content: `## Hello, World!

新建文件 \`hello.cpp\`，输入下面的代码（可以直接在本站运行试试）：

\`\`\`cpp
#include <iostream>      // ① 引入"输入输出流"工具包

int main() {             // ② 主函数：程序从这里开始执行
    std::cout << "Hello, World!" << std::endl;  // ③ 打印一行文字
    return 0;            // ④ 返回 0 = 正常结束
}
\`\`\`

### 逐行讲解（新手必读）

**① #include <iostream>**
- \`#\` 开头的是**预处理指令**，在编译前先执行；
- \`include\` 的意思是"把别的文件的内容复制到这里"；
- \`<iostream>\` 是标准库头文件，提供 cout（输出）和 cin（输入）。
- 类比：去厨房做饭前，先把"锅碗瓢盆"拿出来。

**② int main()**
- 每个 C++ 程序**有且只有一个** main 函数，它是程序的入口；
- \`int\` 表示这个函数最后要返回一个整数给操作系统；
- \`{ }\` 花括号之间是函数体，语句按顺序执行。

**③ std::cout << "..."**
- \`std::cout\` 是标准输出（默认输出到屏幕）；
- \`<<\` 是"输出运算符"，可以理解为"把右边的东西送进屏幕"；
- \`"Hello, World!"\` 是字符串（文字必须用双引号包起来）；
- \`std::endl\` 表示换行（end line）。也可以用 \`"\\n"\`。

**④ return 0;**
- 函数结束，把 0 交还给操作系统，表示"一切正常"；
- 每个语句结尾的**分号 \`;\` 千万别忘**，它是语句的"句号"。

### 你可能会遇到的报错

| 报错现象 | 原因 | 解决 |
|---------|------|------|
| \`expected ';' before ...\` | 少写了分号 | 补上 \`;\` |
| \`'cout' was not declared\` | 没写 \`#include <iostream>\` 或漏了 \`std::\` | 补上头文件 / 加 \`std::\` |
| \`main must return int\` | main 写成了别的类型 | 改回 \`int main()\` |

> 💡 **不必背代码**：先照着敲一遍，把每行都读懂了再改改看（比如改文字内容、加一行输出），
> 改坏了再改回来——改错是学编程最快的方式。`,
    },
    {
      id: 'intro-5',
      title: '新手必知：注释与代码规范',
      content: `## 注释：写给人类看的话

注释是给**未来的你**（和队友）看的说明文字，编译器会完全忽略它。
程序越写越长，没有注释的代码三个月后连作者自己都看不懂 😅

\`\`\`cpp
// 这是单行注释：从 // 到行尾都是注释

/* 这是多行注释：
   可以写很多行，
   以星号斜杠结束 */

#include <iostream>

// 计算并输出两个数的和
int main() {
    int a = 3, b = 4;          // 定义两个变量
    int sum = a + b;           // 求和
    std::cout << sum << std::endl;   // 输出：7
    return 0;
}
\`\`\`

### 新手代码规范（养成好习惯）

1. **命名有意义**：\`age\`、\`studentCount\` 而不是 \`a\`、\`x1\`（小循环变量 i、j 除外）；
2. **缩进对齐**：花括号内的代码统一缩进一个 Tab（4 个空格），层次一目了然；
3. **每行只做一件事**：一行一个语句，别挤在一起；
4. **变量就近声明**：用到之前再定义，不要全部堆在开头；
5. **常量用大写**：如 \`const double PI = 3.14159;\`；
6. **先写注释再写代码**：想清楚这段要干嘛，写下来，再实现。

### 推荐的"学习循环"

1. 📖 读一遍本节的代码；
2. ⌨️ 亲手敲一遍（不要复制粘贴！敲的过程大脑在记忆）；
3. 🔬 改动代码做实验（换个数字、加个输出，猜猜会怎样）；
4. 💥 故意写错，看报错信息长什么样（认识错误是debug的第一步）；
5. ✅ 理解了再进入下一节。

> 💡 本站编辑器里按 **⌘/Ctrl + Enter** 可以直接运行，⌘/Ctrl + S 立即保存。`,
    },
    {
      id: 'intro-6',
      title: '学习路线图（本教程结构）',
      content: `## 跟着这个路线走

本教程共 8 章 + 60 小节，建议**按顺序**学习——每章都建立在前一章之上：

\`\`\`mermaid
简介（你现在在这）
  │
第一章 基础概念 ──── 变量、类型、运算符、if/循环
  │
第二章 函数与作用域 ── 把代码打包复用
  │
第三章 数组与字符串 ── 一次存多个数据
  │
第四章 指针与引用 ──── C++ 的"灵魂"（难但重要）
  │
第五章 类与对象 ────── 面向对象入门
  │
第六章 继承与多态 ──── 面向对象进阶
  │
第七章 异常处理 ────── 让程序更健壮
  │
第八章 STL ────────── 标准模板库（生产力工具）
\`\`\`

### 学习建议

- **第一遍**：每节看懂 + 亲手运行示例，不用背；
- **第二遍**：遮住代码，根据小节标题自己写出来，写不出再看；
- **每天 30 分钟**胜过周末突击 8 小时；
- **卡住了**：把报错信息原样放进搜索引擎，90% 的问题别人都遇到过；
- 第四章（指针）普遍觉得难，**卡住很正常**，先标记跳过，学完第六章再回头看会有豁然开朗的感觉。

### 善用本站功能

| 功能 | 位置 | 用途 |
|------|------|------|
| 目录树 | 左上区域 | 章 → 小节，可折叠 |
| 上一节/下一节 | 内容底部 | 顺序阅读 |
| 运行按钮 | 顶部 ▶ | 运行示例代码 |
| 小/中/大 | 标题栏 | 调整窗口大小 |

> 🎯 目标：学完第八章后，你将能写出结构清晰的小程序，并为学习更深入的主题
> （算法、图形学、游戏开发）打下坚实基础。开始吧！`,
    },
  ],
};
