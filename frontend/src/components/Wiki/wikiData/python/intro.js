// 简介章：6 个分支小节（与 C++ 简介同结构）
export const introChapter = {
  id: 'intro',
  title: '简介',
  sections: [
    {
      id: 'intro-1',
      title: 'Python 是什么',
      content: `## 一条"蟒蛇"的逆袭

Python 由荷兰人 **Guido van Rossum（吉多·范罗苏姆，社区昵称"龟叔"）** 于 1989 年圣诞节期间开始设计，
1991 年发布第一个版本。名字不是来自蟒蛇，而是来自他喜欢的喜剧团体 **Monty Python**（所以 logo 才是蛇）。

你可以这样理解：**Python 是一门"把人类想法直接写下来"的语言**——
不用管类型声明、不用管内存、不用管编译，代码读起来几乎就是英语句子。

### 它厉害在哪里？

- 📖 **好读好写**：同样是打印一句话，Python 一行搞定，C++ 要五六行；
- 🤖 **AI 与数据的第一语言**：PyTorch、TensorFlow、pandas、NumPy 全是 Python 生态；
- 🧰 **"电池自带"**：标准库极其丰富，装第三方库也只要一行 pip；
- 🏆 **长期霸榜**：TIOBE 排行榜常年第一，招聘与教学都在用它；
- 🌍 **哪里都有**：爬虫、自动化脚本、网站后端、量化交易、科学计算、树莓派……

### Python 能做什么（现实例子）

| 领域 | 常用库 | 典型作品 |
|------|--------|---------|
| 人工智能 | PyTorch / TensorFlow | ChatGPT 的训练脚本、人脸识别 |
| 数据分析 | pandas / NumPy | 报表、Excel 自动化 |
| 爬虫 | requests / BeautifulSoup | 采集公开数据 |
| 网站后端 | Django / Flask / FastAPI | Instagram、豆瓣 |
| 自动化 | os / schedule / pyautogui | 批量改文件名、定时任务 |
| 图形小游戏 | pygame / tkinter | 打砖块、贪吃蛇 |

### Python 2 与 Python 3

Python 2 已于 **2020 年 1 月 1 日停止维护**，现在说"Python"一律指 **Python 3**。
本站支持 3.9 / 3.10 / 3.11 三个版本（可在「🧪 Python 环境」里选择），语法基本通用。

> 💡 **和 C++ 的关系**：本站同时提供 C++ 与 Python 教程。
> 想理解"程序到底怎么跑起来的"，两部教程的简介章可以对照看——一个走编译路线，一个走解释路线。`,
    },
    {
      id: 'intro-2',
      title: '程序是怎么运行的（解释 vs 编译）',
      content: `## 计算机只认识 0 和 1

你写的代码人类看得懂，但 CPU 只认识机器指令（一串 0/1）。
把"人类语言"变成"机器语言"有两条完全不同的路线。

### 路线一：编译（C++ 走这条路）

\`\`\`mermaid
源码 main.cpp → [编译器 g++] → 目标文件 → [链接器] → 可执行程序 → 运行
\`\`\`

### 路线二：解释（Python 走这条路）

\`\`\`mermaid
源码 main.py → [解释器 CPython 逐行翻译执行] → 输出
\`\`\`

严格说 Python 是**"先编译成字节码，再由虚拟机执行"**：
你运行 \`.py\` 时，解释器会先把它编译成中间形式（字节码），然后逐条执行。
存放字节码的 \`__pycache__\` 目录就是这么来的（缓存起来，下次快一点）。

### 关键区别

| 对比项 | C++（编译型） | Python（解释型） |
|--------|--------------|-----------------|
| 翻译时机 | 运行前一次性完成 | 运行时逐行翻译执行 |
| 运行速度 | 极快（接近硬件） | 相对慢（约慢 10~100 倍） |
| 错误发现 | 编译期就能拦下语法错误 | 运行到那一行才报错 |
| 发布方式 | 分发可执行文件 | 需要解释器环境 |
| 开发效率 | 低（要写很多样板代码） | 高（几行就能干活） |

### 既然慢，为什么还这么火？

1. **瓶颈不在 Python**：真正耗时的计算都由底层 C/Fortran 库（NumPy、PyTorch）完成；
2. **开发时间更贵**：项目早两周上线，比程序快 30% 更值钱；
3. **胶水语言**：能把各种 C/C++ 库、系统命令、网络服务粘在一起。

> 💡 **本站两种运行方式，先搞清楚区别**：
> - **▶ 运行**：把代码交给 Piston 沙箱批量执行，适合**命令行程序**（读取输入、打印结果），
>   可绑定你在「🧪 Python 环境」里配置的库；
> - **💻 终端运行**：开一个真正的交互式终端（可中途 \`input()\`、边跑边输入），
>   \`tkinter\` / \`pygame\` 这类**图形窗口程序必须用它**，再点面板里的「🪟 窗口」看画面。
> 终端跑在独立的沙箱环境里（Python 3.11 + 预装 pygame），点终端面板的「ⓘ 沙箱」可以看到实际可用的库。`,
    },
    {
      id: 'intro-3',
      title: '环境安装',
      content: `## 方式一：用本站（零安装，推荐新手）

新建一个 \`.py\` 文件就能直接运行：

- 命令行小程序 → 点顶部 **▶ 运行**；
- 需要中途输入、或有图形窗口 → 点 **💻 终端运行**。

不需要装任何软件，iPad 也能写。

## 方式二：本地安装（想离线学习再装）

### Windows

1. 打开 <https://www.python.org/downloads/> 下载安装包；
2. **安装时务必勾选 "Add Python to PATH"**（不勾的话命令行找不到 python）；
3. 验证：打开 PowerShell 输入 \`python --version\`，显示 \`Python 3.x.x\` 即成功。

### macOS

- 系统自带 Python 3（版本可能偏旧）；
- 想要新版：\`brew install python\`（需先装 Homebrew）。

### Linux

\`\`\`bash
sudo apt install python3 python3-pip   # Ubuntu / Debian
sudo dnf install python3 python3-pip   # Fedora
\`\`\`

## 方式三：编辑器 / IDE 推荐

| 工具 | 适合 | 说明 |
|------|------|------|
| VS Code | 大多数人 | 免费，装 "Python" 扩展即可 |
| PyCharm | 项目开发 | 功能全，社区版免费 |
| 本站编辑器 | 入门 / iPad | 开箱即用，无需配置 |

## 本站的「Python 环境」（特色功能）

在项目列表页点 **🧪 Python 环境**，可以创建自己的云端环境：

1. 选择 Python 版本（3.9 / 3.10 / 3.11）；
2. 每行填一个第三方库（如 \`numpy\`、\`requests\`、\`pandas\`），后台会自动 pip 安装；
3. 回到工作区，用工具栏的 **🐍 环境** 下拉把它绑定到当前项目（打开 \`.py\` 文件时才显示）；
4. 之后点 **▶ 运行** 就会使用这个环境的版本与库。

> ⚠️ **别混淆两套环境**：自定义环境只作用于 **▶ 运行**；
> **💻 终端运行** 跑在固定的沙箱镜像里（Python 3.11 + 预装 pygame/tkinter）。
> 终端里缺的库，可以请管理员在后台「🧪 沙箱库」里添加。`,
    },
    {
      id: 'intro-4',
      title: '第一个程序逐行解读',
      content: `## Hello, World!

新建文件 \`hello.py\`，输入这一行（在本站可以直接运行）：

\`\`\`python
print("Hello, World!")   # 打印一行文字
\`\`\`

就这一行，没有 \`main\`、没有分号、没有 \`#include\`。下面是它为什么这么短。

### 逐行讲解（新手必读）

**① print 是什么**

- \`print\` 是 Python 内置的**输出函数**，作用是"把括号里的东西显示到屏幕上"；
- 括号里可以放文字（字符串）、数字、变量，甚至多个值：\`print(1, 2, 3)\` → \`1 2 3\`；
- 它是函数，所以**括号不能省**，写成 \`print "hi"\` 会直接报语法错误。

**② "Hello, World!" 是什么**

- 用引号包起来的一串字符叫**字符串**（string）；
- 单引号 \`'...'\` 和双引号 \`"..."\` 完全等价，选一种用到底即可；
- 引号里的内容**原样输出**，包括空格和标点。

**③ 注释**

- \`#\` 后面的内容是给人看的，Python 直接忽略；
- 用它可以解释代码在做什么——三个月后的你会感谢现在的你。

### 换行、多值输出

\`\`\`python
print("第一行")
print("第二行")            # 每次 print 默认换行
print("A", "B", "C")       # 多个值用空格分隔：A B C
print("不换行", end="")     # end 参数改结尾字符，默认是 \\n
print("接着打印")
\`\`\`

### 你可能会遇到的报错

| 报错 | 原因 | 解决 |
|------|------|------|
| \`SyntaxError: invalid syntax\` | 少了括号/引号，或用了中文标点 | 检查 \`()\` \`""\` 与英文标点 |
| \`IndentationError: unexpected indent\` | 行首多了空格（Python 靠缩进分层） | 删掉多余缩进，顶格写 |
| \`NameError: name 'pritn' is not defined\` | 名字拼错 | 改成 \`print\`（注意大小写） |
| \`SyntaxError: EOL while scanning string literal\` | 引号没配对 | 补上结尾引号 |

> 💡 **不必背代码**：先照敲一遍，再改改看（改文字、加一行、把 print 换成计算），
> 改错是学编程最快的方式。编辑器里按 **⌘/Ctrl + Enter** 可直接运行。`,
    },
    {
      id: 'intro-5',
      title: '新手必知：注释、缩进与代码规范',
      content: `## 注释：写给人类看的话

\`\`\`python
# 这是单行注释：从 # 到行尾都被忽略

"""
这是多行字符串，
常被当作多行注释使用。
"""

# 计算两个数的和
a = 3
b = 4
print(a + b)      # 输出 7
\`\`\`

### 缩进：Python 的"语法"

C++ 用 \`{ }\` 划分代码块，**Python 用缩进**。这不是风格问题，是语法问题：

\`\`\`python
if a > 2:
    print("大于 2")     # 4 个空格缩进 = 属于 if 内部
    print("仍然在内部")
print("回到外面")       # 顶格 = 不属于 if
\`\`\`

**规则**：同一层的代码缩进必须完全一致；官方推荐 **4 个空格**（不要混用 Tab 和空格）。

### PEP 8：Python 官方代码风格（重点记住前 5 条）

| 项目 | 规范 | 例子 |
|------|------|------|
| 变量 / 函数名 | 小写 + 下划线（snake_case） | \`student_count\` |
| 类名 | 大驼峰（PascalCase） | \`StudentInfo\` |
| 常量 | 全大写 | \`MAX_SIZE = 100\` |
| 缩进 | 4 个空格 | —— |
| 每行长度 | 不超过 79~100 字符 | 太长就换行 |
| 运算符两侧 | 加空格 | \`a = b + 1\` |
| 逗号后 | 加一个空格 | \`f(a, b)\` |

### 好的命名 vs 差的命名

\`\`\`python
# ❌ 看不懂：三个月后连你自己都不知道 a、b 是什么
a = 18
b = "小明"

# ✅ 一眼看懂
age = 18
student_name = "小明"
\`\`\`

变量名规则：只能用**字母、数字、下划线**，不能以数字开头，区分大小写，
不能用关键字（\`if\`、\`for\`、\`class\`、\`None\`、\`True\`……）。

### 推荐的"学习循环"

1. 📖 读一遍本节的代码；
2. ⌨️ 亲手敲一遍（不要复制粘贴）；
3. 🔬 改动代码做实验，先猜结果再运行；
4. 💥 故意写错，认识报错信息的样子；
5. ✅ 理解了再进入下一节。

> 💡 小技巧：在终端里 \`python3 -i main.py\` 会在程序结束后进入交互模式，
> 所有变量都还在，可以接着敲表达式做实验（本站「💻 终端运行」可直接体验）。`,
    },
    {
      id: 'intro-6',
      title: '学习路线图（本教程结构）',
      content: `## 跟着这个路线走

本教程共 8 章 + 60 余小节，建议**按顺序**学习——每章都建立在前一章之上：

\`\`\`mermaid
简介（你现在在这）
  │
第一章 基础概念 ────── 变量、数字与字符串、输入输出、条件与循环
  │
第二章 列表·元组·字典 ── 一次存一批数据（Python 的看家本领）
  │
第三章 函数与作用域 ──── 把代码打包复用
  │
第四章 字符串与文本 ──── 文本处理、格式化、正则入门
  │
第五章 文件与异常 ────── 读写文件、让程序不崩
  │
第六章 模块与标准库 ──── import、常用库、pip 与虚拟环境
  │
第七章 面向对象 ──────── 类、继承、魔术方法
  │
第八章 进阶与实战 ────── 生成器、装饰器、图形程序、综合练习
\`\`\`

### 各章难度与重点

| 章节 | 难度 | 学完能做什么 |
|------|------|-------------|
| 一 ~ 二 | ⭐ | 写几十行的小脚本，处理一批数据 |
| 三 ~ 五 | ⭐⭐ | 把程序拆成函数、读写文件、处理报错 |
| 六 | ⭐⭐ | 用标准库和第三方库解决实际问题 |
| 七 ~ 八 | ⭐⭐⭐ | 写出结构清晰、可维护的中型程序 |

### 学习建议

- **第一遍**：每节看懂 + 亲手运行示例，不用背语法；
- **第二遍**：遮住代码，根据小节标题自己写出来；
- **每天 30 分钟**胜过周末突击 8 小时；
- **卡住了**：把报错最后一行原样丢进搜索引擎，90% 的问题别人都遇到过；
- 第七章（面向对象）普遍觉得抽象，**先会用再理解**，回头再看会豁然开朗。

### 善用本站功能

| 功能 | 位置 | 用途 |
|------|------|------|
| ▶ 运行 | 工具栏 | 跑命令行程序（可用自定义环境） |
| 💻 终端运行 | 工具栏 | 交互式运行 / \`input()\` 边跑边输 / 图形程序 |
| 🪟 窗口 | 终端面板 | 查看 tkinter / pygame 的图形界面 |
| ⓘ 沙箱 | 终端面板 | 查看终端里可用的 Python 与预装库 |
| 🧪 Python 环境 | 项目列表页 | 选版本、装第三方库（作用于 ▶ 运行） |
| 目录树 | 左上区域 | 章 → 小节，可折叠 |
| 上一节/下一节 | 内容底部 | 顺序阅读 |

> 🎯 目标：学完第八章后，你能独立写出 100~300 行的实用程序
> （数据处理、自动化脚本、小游戏），并为 Web / 数据分析 / AI 等方向打好基础。开始吧！`,
    },
  ],
};
